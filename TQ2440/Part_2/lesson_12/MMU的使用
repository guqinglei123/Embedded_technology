参看书籍:
文档——Uboot启动流程分析（汇编）
《嵌入式LINUX应用开发手册》 第7章（汇编）
https://blog.csdn.net/lvjianxin6015/article/details/56292208
https://blog.csdn.net/jus3ve/article/details/80730021
主要内容：
1.MMU的功能解析
    １).负责物理地址到虚拟地址的映射。直接查看程序运行的地址会发现不同程序运行于相同的地址，
        这是因为该地址为虚拟地址(这个起始地址该是多少，在哪里设置呢？)。
    ２）.负责硬件上内存访问权限的检查
２.地址类型及转换方式：
    类型：虚拟地址(VA,CPU使用的地址),变换后的虚拟地址(MVA，cache以及ＭＭＵ使用的地址。
    当ＭＶＡ < 32M,  MVA = VA | (PID << 25),ｅｌｓｅ　MVA = VA),物理地址(PA，外设的实
    际地址，通过ＭＭＵ将ＭＶＡ转化)
    ＭＭＵ将ＭＶＡ转化为ＰＡ的方式：a.通过数学公式转化，ｂ.通过表格储存(页表)，页表可以是多级的
    (ＡＲＭ 中最多采用了二级页表)
３.ＡＲＭ中，ＭＭＵ将ＭＶＡ转化为ＭＭＵ的过程
    ＡＲＭ中存在两种页表转化方式：ａ.仅仅使用一级页表转换的，段转化。ｂ.使用两级页表转化的，页
    转化（粗页转化，细页转换）。
    转化过程为：
    1).通过ＭＶＡ[31:20]和TTB_base(储存在CP15的C2寄存器中)索引一级页表，得到一级描述符(4kb),确定
    确arm的转化方式。
    ２－１)如果是段转化，通过一级描述符得到section_base ,通过section_base+ ＭＶＡ[31:20]+为００
    低两位得到段描述符。
    ２－２）段描述符的[３１：２０]＋ＭＶＡ[１９:０]得到ＰＡ
    ３－１）如果是粗页转化，通过一级描述符得到section_base
４.ＭＭＵ的配置和使用：

５.cache的作用：

